# OpenAI Codex CLI detection rules.
id = "codex"

[[rule]]
priority = 100
state = "blocked"
any_of = [
    "allow command",
    "approve this command",
    "yes (y)",
    "always approve",
    "grant access",
]

[[rule]]
priority = 90
state = "working"
# "thinking" alone matches transcript reasoning headers long after the
# task ends — only the live spinner form counts.
any_of = ["esc to interrupt", "working (", "thinking ("]

[[rule]]
priority = 50
state = "idle"
any_of = ["⏎ send", "send   ⇧⏎ newline", "/ commands"]
